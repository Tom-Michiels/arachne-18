# Tested local Python 3.12 environment (separate from simulation/requirements.txt).
numpy==2.5.2
mujoco==3.12.0
better-actuator-models==1.0.2
mlx==0.32.2; sys_platform == 'darwin'
imageio>=2.37
imageio-ffmpeg>=0.6
pillow>=11
# Training additionally needs _mjmlx_native from MuJoCo-MLX-Cpp; see README.md.
